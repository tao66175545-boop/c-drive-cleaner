# C盘智能清理 v1.4.7

## 版本信息

- 左侧边栏底部将“本地工具”调整为“版本号 vX.Y.Z”，让用户可以直接确认当前程序版本。
- 界面版本号统一从 `version.json` 读取，后续发布提升版本时会自动同步显示，不再单独维护界面文字。
- 版本文件缺失或格式异常时显示“版本号 未知”，不影响主程序启动和本地扫描。
- 版本号标签与侧栏导航按钮统一使用 12px 外边距和 88px 内容宽度，文字在固定容器内居中，消除左右留白不对称。
- 版本号标签下移到距侧栏底部 8px 的稳定位置，减少底部无效留白。

## 运行日志

- 修复 UTF-8 子进程日志被 UI 按系统默认 GBK 编码读取而产生的中文乱码。
- 日志增量读取改用严格 UTF-8 快照；遇到正在写入的未完整多字节字符时等待下一轮读取，避免显示替换字符。
- 所有后台 PowerShell 任务通过固定 UTF-8 宿主启动，确保中文标准输出在进入日志文件前即使用 UTF-8 编码，同时保留参数数组边界。

## 飞猪旅行搜索

- FlyAI 子进程会自动继承 Windows 当前用户的静态代理设置，修复浏览器可访问但 Node 直连发生 TLS 重置的问题。
- AI 旅行搜索预算由 45 秒调整为 90 秒，并对快速发生的 TLS 或网络瞬时错误自动重试一次。
- TLS、网络不可达和真实超时分别显示针对性提示，不再统一误报为搜索超时。
