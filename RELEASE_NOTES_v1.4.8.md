# C盘智能清理 v1.4.8

## 对话控制

- 智能助手新增确定性的本地自然语言控制，可切换概览、清理清单、运行日志和助手页面。
- 支持开始或停止只读扫描、勾选低风险建议项、清空选择、查看最新报告、查看清理前后变化和打开受限系统设置。
- 已配置云端模型时，明确的本地控制指令也优先在本机解析和执行，减少等待并避免不必要的网络请求。

## 安全边界

- 所有对话控制复用既有严格 UI Action Broker，不新增任意 Windows 控制、路径输入或命令执行能力。
- 勾选只能使用当前扫描结果中的稳定项目 ID；微信和 QQ 用户媒体仍为谨慎项，不会被低风险建议自动勾选。
- “开始清理”只会准备建议项并打开原生确认框，智能助手不能替用户点击最终确认。
- 否定指令、路径、Shell/PowerShell、绕过规则和代替确认请求会失败关闭，且不改变当前界面状态。

## 架构与验证

- 新增 `P15-conversational-ui-control` 架构阶段和口语指令契约测试。
- 覆盖中英文指令映射、否定语义、危险请求、Broker 参数校验、稳定 ID 选择和最终确认边界。
