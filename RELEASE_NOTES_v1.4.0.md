# C盘智能清理 v1.4.0

## 新增

- 可选的 BYOK 云端 AI Agent，支持 Responses、Chat Completions 和纯文本兼容接口。
- 助手页面提供流式回答、停止请求、供应商配置和一键移除本机凭据。
- Agent 可通过严格工具契约读取脱敏扫描摘要、切换页面、启动或取消扫描，并修改可撤销的清单勾选。
- Agent 可打开原生清理确认，但最终是否执行始终由用户在界面中决定。

## 安全与隐私

- API Key 使用 Windows DPAPI CurrentUser 加密，不写入项目、配置 JSON、日志、命令行或发布包。
- 云端模式默认关闭，首次启用及供应商配置变化后必须重新授权。
- 云端只接收用户问题和重新构造的字段白名单摘要，不接收原始路径、用户名、机器名、文件内容或运行日志。
- 路径、命令、凭据、提示注入和替代最终确认的请求会在联网前拒绝。
- 单轮最多 6 次模型请求、8 次工具调用；重复调用 ID 拒绝执行。
- 无配置、断网或模型不支持工具时，现有离线助手、扫描和清理仍可使用。

## 验证

- 新增 Agent 运行时、协议、隐私快照、工具重放、供应商能力档位和 UI 流式夹具测试。
- 完整项目验证覆盖安全自检、计划契约、扫描、增量索引、执行代理、离线助手、UI 和发布包。
