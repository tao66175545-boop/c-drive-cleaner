# C盘智能清理 v1.4.6

## 对话式清理

- 用户可在智能助手中输入明确指令，让助手先执行只读扫描、自动勾选低风险建议项，并打开原生清理确认框。
- 助手只处理本次扫描结果中的稳定项目 ID，且只自动选择 `Recommended` 项；微信/QQ 图片与视频等谨慎项目不会自动勾选。
- 否定、取消或停止清理的表达不会被识别为执行授权，最终删除仍必须由用户在原生确认框点击“是”。

## 用户头像

- 智能助手中的用户头像替换为用户上传的固定头像，并保留旧头像作为资源缺失时的兼容回退。

## 飞猪旅行建议

- 新增可选的飞猪 FlyAI 只读旅行搜索，可规划行程并查询酒店、机票、火车和景点建议。
- 首次使用时明确征求本次会话授权，只发送当前旅行问题，不发送扫描结果、文件路径、日志、凭据或清理计划。
- 旅行能力运行在独立进程中，不执行预订、支付，也不能调用清理工具。

## 发布可靠性

- 源码清单改用固定 Ordinal 路径排序，消除中文 Windows 与 GitHub 英文 Windows 区域设置不同导致的候选指纹误报。

## 对话界面

- 统一助手与用户气泡的文字布局：水平内边距 `14px`、垂直内边距 `10px`，单行和多行文本均垂直居中且左右等距。
