# C盘智能清理 v1.1.2

发布日期：2026-08-19

## 版本识别修复

- 将 `version.json` 纳入运行包，安装后的程序可以可靠识别自身版本与更新通道。
- CI 会打开实际 ZIP 并确认 `version.json` 存在，避免未来打包规则回归。
- 延续 v1.1.1 的 ASCII Release 资产命名，下载 URL、SHA-256 与实际附件保持一致。

## 核心改进

- 稳定清理目标 ID 和 v2 清理计划。
- 规则哈希、30 分钟有效期、快照完整性与用户内容变化校验。
- 报告与程序目录分离。
- 最小权限两阶段自动发布。
