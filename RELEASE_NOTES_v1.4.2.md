# C盘智能清理 v1.4.2

## 修复

- 修复清理清单右上角大小标签贴住卡片顶部、遮挡圆角边框并形成白色矩形缺口的问题。
- 统一所有清理项行的顶部 `10px` 和右侧 `14px` 安全边距，窗口缩放与页面切换后仍保持一致。
- 大小标签改为透明背景，保留父级圆角卡片的连续边框与抗锯齿表面。

## 质量保障

- 新增清理项行几何回归测试，检查顶部安全区、右侧边距和透明背景。
- 新增 `DrawToBitmap` 像素检查，防止子控件再次遮挡卡片顶部边框。
- 将圆角容器子控件安全区写入 UI 组件规范，并审查其他圆角容器的同类布局。
