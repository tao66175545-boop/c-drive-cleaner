# C 盘清理 UI 设计规则

## 设计方向

- 工具型桌面应用：克制、清晰、可扫描，不使用装饰性大面积渐变。
- 视觉层级固定为：白色品牌/操作层、浅灰工作区、白色内容卡片、深色日志输出。
- 曙光红只用于主操作、状态提示和重要结果；灰色用于边框、辅助信息和不可用状态。

## 布局基线

- 主工作区外边距统一为 `18px`。
- 卡片内部常用间距为 `16px`，标题与说明使用 `12px` 垂直节奏。
- 卡片与按钮统一 `8px` 圆角，边框统一 `1px`。
- 顶部栏固定 `74px` 高度，顶部和底部各使用一条同色系分隔线。
- 统计卡标题、色条必须使用固定高度和共同中心线，不用 AutoSize 参与垂直对齐。
- 状态圆点与状态文字必须放在同一个横向容器中，通过 Margin/Alignment 对齐。

## 组件原则

- `CDriveRoundedPanel`：只负责卡片/轨道的圆角、背景和边框，不在业务代码中重复绘制。
- `CDriveRoundedButton`：唯一的可点击按钮组件，继承 `Control` 而非 WinForms `Button`；按钮尺寸由操作栏统一分配。它在组件内部统一绘制圆角、文字、描边与默认、选中、悬停、按下、禁用状态。
- 禁止在业务层使用 `FlatStyle`、`FlatAppearance`、`UseVisualStyleBackColor`，也禁止通过 `Region` 裁剪按钮圆角。按钮只能配置组件公开的状态颜色，不能混用系统按钮绘制链路。
- 圆角路径必须以 `0.5px` 内缩描边并开启抗锯齿；四角由父容器背景清底后绘制，避免系统边框残影、半像素溢出和方形像素角。
- 导航激活态使用 `Selected` 及其专属颜色，不通过视图切换时临时覆写默认、悬停颜色；交互状态优先级固定为禁用 > 按下 > 悬停 > 选中 > 默认。
- `New-StatCard`：统一标题、数值、辅助说明三段结构，标题和说明使用固定文本区域。
- 扫描结果必须进入独立的“清理清单”视图：每行只展示程序内置、已扫描到的固定安全目标，并含复选框、占用、建议等级和影响说明。默认不勾选；“勾选建议项”是显式、可撤销的辅助动作。
- 清理清单协议使用 v2：必须携带扫描编号、扫描时间、规则清单哈希和项目大小快照；UI 回传选择时必须原样保留这些字段。
- 执行清理前必须汇总用户已选数量和预计释放空间；主操作文案使用“执行所选项”。UI 只回传稳定项目 ID，禁止回传或拼接用户输入的文件路径。
- 顶部操作按钮统一 `104 x 36px`，间距统一 `8px`；主按钮通过填充色区分，次要按钮保持同一几何样式。
- 进度条由轨道和填充条组成，填充条在轨道内缩 `1px`，左右外边距由父卡片动态计算，禁止写死宽度。
- 统计卡底部说明使用独立底部条，左右内边距固定 `12px`；宽度必须在父卡片布局后动态计算，禁止使用会贴边的固定宽度或错误 Anchor。

## 数据与显示

- 统计值必须来自实时 `DriveInfo`，禁止用固定示例值替代运行时数据。
- 磁盘状态条：红色为已用，灰色为可用。
- 清理前后对比条：表示可用空间占 C 盘总容量比例。
- `0%` 必须显示为零宽度，等待状态不显示误导性的残留填充线。
- Logo 静止状态必须显示完整品牌图，不能直接使用逐元素入场动画的 `t=0` 序列帧；静止图必须直接复用动画序列最后的稳定帧，确保结束帧与静止帧的像素位置完全一致。交互播放使用浏览器按 SVG 时间轴预渲染的 135 帧（60fps、2.22 秒），以真实经过时间选帧。禁止通过固定递增帧号或任意定时器间隔近似源动画速率。
- 深色日志输出必须由独立内容容器提供最少 `10px` 水平、`8px` 垂直内边距；TextBox 不得直接贴到外框。
- 操作栏中的固定尺寸按钮必须用 `(容器高度 - 按钮高度) / 2` 实时计算 `Top`，禁止用固定 `Top` 值假定容器高度。
- 统计卡说明条在每次布局后依据卡片实际 `ClientSize` 锚定在底部，标题、数值、说明条必须使用互不重叠的区域。
- 顶部栏与页面内容必须放入不同的布局层：页面内容只能位于内容宿主中，内容宿主的 `Top` 必须等于顶部栏的 `Bottom`。切换视图只切换内容宿主的子视图，禁止通过 `BringToFront` 让内容覆盖顶部栏。
- 文本超出区域时优先调整布局或换行，不允许通过负坐标、覆盖或横向滚动解决。

## 修改检查清单

1. 是否复用了现有组件和颜色规则，而不是新写一套边框/圆角？
2. 是否在常规尺寸和最小尺寸下验证了对齐、溢出和文字可读性？
3. 是否连续切换概览、清理清单和日志页，验证所有页面均从顶部栏下方开始，并验证按钮默认、选中、悬停、按下、禁用状态？
4. 是否运行 UI 解析检查、窗口烟测和 `C-Drive-Cleaner.ps1 -SelfTest`？
5. 是否检查了圆角四角像素、统计卡底部说明边距、日志首行内边距、清单操作按钮中心线和控件实际 ClientSize？按钮不得出现系统默认描边或方形角。
6. 是否验证扫描后默认无勾选、建议项可勾选/撤销、空选择不可执行，以及未知项目 ID 会被清理器拒绝？
7. 是否验证过期计划、规则哈希变化、快照缺失和用户内容变化都会安全中止，并在 UI 中显示为中止而不是完成？
