# C盘智能清理 v1.4.11

## 清理清单全选

- 扫描完成后，清理清单操作栏新增“全选”按钮，可一次选中本次扫描列出的全部候选项。
- 全选只操作当前扫描清单中的固定项目 ID，不接受或生成任意文件路径，也不会直接启动清理。
- 微信和 QQ 媒体等谨慎项继续默认不勾选；只有用户主动点击“全选”时才会进入选择状态。
- 全部项目已选中或任务运行时，“全选”按钮会禁用；“清空选择”和逐项取消仍可随时撤销选择。
- 最终清理继续复用原生确认框，用户确认前不会删除任何文件。

## 界面一致性

- “勾选建议项 / 全选 / 清空选择”使用统一的按钮圆角、30px 高度和 8px 间距。
- 清单汇总文字会根据按钮区域自适应宽度，避免窄窗口下与批量操作按钮重叠。
