# C盘智能清理在线升级方案

## 结论

可以通过 GitHub Releases 向用户分发最新版本。公开仓库的 Release 附件可匿名下载，客户端可使用 GitHub API 的 `releases/latest` 查询最新版本。

但当前项目是以 `C盘清理.bat` 启动的 PowerShell 脚本集合，尚缺少版本标识、发布清单、代码签名、独立更新器和回滚机制。现阶段适合“手动下载最新 ZIP”；在完成下述安全能力前，不应做静默自动覆盖更新。

## 发布架构

```text
GitHub Release
  ├─ C盘智能清理工具包-1.2.0.zip
  ├─ release.json
  ├─ release.json.sig
  └─ 更新说明
        ↓ HTTPS
应用内“检查更新”
        ↓ 校验版本、签名、SHA-256
下载到临时目录
        ↓
独立 Updater.exe 完成替换
        ↓
versions/1.2.0 启动新版本，失败则回滚 1.1.0
```

## 必要组件

| 组件 | 职责 | 关键要求 |
| --- | --- | --- |
| `version.json` | 当前安装版本与通道 | 例如 `1.2.0`，不使用文件修改时间判断版本。 |
| `release.json` | 最新版本、下载地址、SHA-256、发布日期、最低兼容版本、更新说明 | 只作为元数据，必须与签名文件一起验证。 |
| `release.json.sig` | 发布清单的离线签名 | 客户端内置公钥；不能只相信同一 GitHub 页面上的 SHA-256。 |
| `Updater.exe` | 下载后替换版本目录、重启、回滚 | 必须独立于主程序运行，不能由正在使用的脚本自我覆盖。 |
| 版本目录 | 并存当前与上一个版本 | 用户报告、配置和日志放在 `%LocalAppData%`，不放在安装目录。 |

## 更新流程

1. 用户点击“检查更新”，默认不在后台自动联网。
2. 客户端读取 GitHub Release 的 `release.json`，比较语义版本号。
3. 验证 `release.json.sig`，再下载 ZIP 并校验 SHA-256。
4. 显示版本号、体积、更新说明与“立即更新/稍后更新”选择。
5. `Updater.exe` 等待主窗口退出，将解压后的新版本写入新版本目录。
6. 启动自检；成功后切换当前版本指针，失败则恢复上一个版本并提示原因。

## 安全与发布要求

- 发布包与更新器应使用 Authenticode 代码签名；公开发布不应依赖 `-ExecutionPolicy Bypass` 作为长期启动方案。
- Release 清单必须有独立签名。仅使用 HTTPS、GitHub API 或同源 SHA-256，无法抵御发布仓库被篡改后的供应链风险。
- 更新器仅替换白名单文件，禁止接受服务器下发的任意删除路径或脚本命令。
- 不上传磁盘路径、清理清单、日志或用户文件；更新检查只请求版本元数据。
- GitHub 在部分网络环境可能访问不稳定，应预留可配置镜像/CDN 地址，并保留手动下载 ZIP 的入口。

## 发布流水线

1. GitHub Actions 在 tag（如 `v1.2.0`）触发构建。
2. 运行 PowerShell 解析、`-SelfTest` 与 UI 冒烟测试。
3. 生成 ZIP、SHA-256、`release.json`，使用受保护的签名服务签名。
4. 上传 Release 资产和更新说明。
5. 人工复核签名、安装与回滚后再将 Release 标记为 Latest。

## 当前阶段建议

先上线 GitHub Releases 的手动下载页和版本化 ZIP；随后开发签名的 `Updater.exe` 与应用内“检查更新”。需要确定 GitHub 仓库归属、发布域名/镜像、代码签名证书主体后，才能进入实际发布和自动升级开发。
