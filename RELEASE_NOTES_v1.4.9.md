# C盘智能清理 v1.4.9

## 飞猪旅行结果

- 旅行规划从普通长文本气泡升级为宽版结构化结果，按摘要、交通、景点、住宿、预算、总结和实时推荐分区展示。
- 本地解析 FlyAI Markdown，移除 `##`、`**` 和长 URL 等原始标记；推荐详情统一为可点击的“飞猪详情”。
- 所有旅行正文继续支持鼠标选择、`Ctrl+C` 和右键复制。
- 同一旅行问题可在隔离 Travel Host 中追加一次有上限的关键词搜索，最多补充 3 张飞猪/阿里 CDN 官方预览图。

## 安全与降级

- 不执行供应商 HTML 或 Markdown，只渲染本地类型化显示模型。
- 详情链接只接受 HTTPS；图片只接受可信阿里 CDN，并限制数量、大小、超时、内容类型和解码尺寸。
- 图片下载或解码失败时自动使用完整文字卡片，不影响主行程，也不会显示破图框。
- 飞猪仍为只读搜索，不会自动预订、下单或提交旅客信息，且与 C 盘清理数据隔离。

## 体验与验证

- 旅行卡片遵循现有红白灰视觉体系、统一 `8px` 圆角和固定间距。
- 常规与紧凑窗口均重新测量文本和图片区，避免横向滚动、卡片越界和文字截断。
- 新增 Markdown 解析、可信链接/图片、无图降级、可复制文本和紧凑布局测试。
