# C盘智能清理 v1.4.3

## 修复

- 修复部分 OpenAI Responses 兼容网关把工具参数重复拼接为 `{}{}` 或 `{}{...}`，导致工具回传后的第二轮请求返回 `HTTP 502 / server_error` 的问题。
- 工具参数在模型响应解析与下一轮请求发送前均经过结构化 JSON 归一化，覆盖 Responses、Chat Completions、流式和非流式路径。
- 兼容逻辑只接受重复空对象、一个非空对象加空对象，或完全相同的重复对象；两个不同的非空对象会失败关闭，避免掩盖冲突参数。

## 验证

- 新增合法参数、重复空对象、空对象加有效参数、相同重复参数和冲突参数的回归测试。
- 使用当前 `grok-4.6` Responses 网关复现原始两轮工具调用：`get_app_state`、`get_scan_summary` 与后续 `start_scan` 均返回合法参数，第二轮请求成功完成。
- API Key、原始路径和用户数据仍不进入命令行、请求日志或发布包。
