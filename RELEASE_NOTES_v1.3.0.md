# C盘智能清理 v1.3.0

## 真正的增量扫描

- 新增原生 NTFS USN Journal 读取器，不再只做能力探测；首次扫描建立稳定 ID 增量索引，后续根据 USN 变更只重扫受影响项目。
- 增量索引不保存路径、文件名、用户名或文件内容；微信/QQ 用户内容规则明确排除在索引之外。
- 索引包含完整性哈希和容量预算：最大 64 MB、100 个项目、单项 25 万身份、总计 100 万身份，超过预算自动回退。
- Journal 无权限、换代、回卷、记录不兼容、索引损坏或非 NTFS 环境均自动回退到已验证的快速文件系统扫描。
- 清理阶段完全绕过增量缓存，仍按本次扫描计划重新核验稳定 ID 与当前大小。

## 性能与体验

- 真实 3000 文件测试集的无变化重复扫描基准超过 30 倍，验收目标为至少 3 倍。
- “开始扫描”改为快速清理扫描，默认跳过全用户目录空间剖析，避免深度诊断掩盖增量扫描收益。
- UI 显示当前使用增量索引、首次建索引或快速兼容扫描，并在日志中报告复用与重扫项目数。

## 兼容性

- Windows PowerShell 5.1、WinForms 和现有稳定 ID/计划/执行代理契约保持不变。
- 不自动请求 UAC；普通权限用户无法读取 USN Journal 时仍能使用完整扫描与清理功能。
- GitHub Windows Runner 原生集成验证已实际创建文件，并从 USN Journal 观察到对应变更记录。
