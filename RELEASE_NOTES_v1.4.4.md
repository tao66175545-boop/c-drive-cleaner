# C盘智能清理 v1.4.4

## 智能助手对话体验

- 将单一文本记录区重构为左右对齐的双人聊天气泡，助手在左、用户在右。
- 使用曙光云 IP 的挥手形象作为助手头像，并提供 6 套本地通用用户头像；每次启动随机选择一次。
- 流式响应只更新当前助手气泡，避免消息跳动，并在窗口缩放后重新计算换行和高度。
- 保留非视觉文本记录，方便无障碍读取、自动化验证和故障诊断。

## 安全边界

- AI 仍只能解释扫描结果或建议稳定项目 ID，不能提交路径、命令或代替用户确认清理。
- 扫描不删除文件；清理仍只执行用户在清单中明确勾选并经原生确认的项目。
- 微信、QQ 用户媒体继续独立列出且默认不勾选。

## 验证

- 新增气泡左右对齐、头像加载、流式单气泡、文本裁切和响应式宽度测试。
- 完整项目验证覆盖自测、Agent 合约、UI 冒烟、打包和发布策略。
- 候选发布脚本对 GitHub EOF、连接重置、超时和 5xx 增加有限退避重试，减少上传过程中的人工介入。
