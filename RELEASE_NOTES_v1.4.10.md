# C盘智能清理 v1.4.10

## 飞猪空结果恢复

- 修复 FlyAI 体验模式偶发返回成功状态但旅行数据为空时，界面只能提示“没有可展示的结果”的问题。
- 主行程搜索返回空数据时自动进行一次关键词搜索降级，展示真实景点、门票或酒店推荐卡片，无需用户重复输入。
- 严格区分有效 Markdown、有效推荐列表与空数据；空字符串和空列表不再作为成功结果进入 UI。

## 界面修复

- 修复左侧版本号区域覆盖侧栏右边界、形成白色底溢出的瑕疵。
- 侧栏分隔线固定占据完整高度，并增加底部像素回归检查，确保页脚和其他组件不能遮挡边界。
- 修复飞猪回答中的 emoji 被不完整清除后留下方框缺字符号；现在统一移除装饰性 emoji 序列、变体选择符和零宽连接符，同时保留中文、数字与路线箭头。

## 安全边界

- 旅行降级仍在隔离 Travel Host 中执行，仅发送当前旅行问题，不读取或传输扫描结果、路径、日志和清理计划。
- 飞猪功能继续保持只读，不会自动预订、下单或提交旅客信息。
