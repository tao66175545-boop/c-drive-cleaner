# C盘智能清理 v1.2.0

## 架构升级

- 清理与诊断目标迁移到版本化规则目录，规则具有稳定 ID、风险等级、默认选择和建议契约。
- 新增版本化 NDJSON 事件协议，为后续实时进度、取消、C# Core 和智能助手提供结构化接口。
- 新增机器可执行的架构 LOOP，按阶段验收规则、事件、AI 权限边界和原有安全测试。
- 新增 AI 工具白名单契约；智能助手不能接收任意路径、执行命令或代替用户确认删除。
- 新增智能助手页面：离线解释扫描结果、按稳定 ID 提供低风险勾选建议；应用建议不会自动清理。
- 新增快速目录扫描提供器，热扫描基准超过原 PowerShell 枚举 20 倍，同时保留显式回退路径。
- 新增托管子进程、结构化进度和取消，实测取消确认约 8-9 毫秒。
- 新增执行代理和持久化 NDJSON 操作日志，未知 ID、无扫描计划和过期/变化计划均失败关闭。

## 兼容性与安全

- 保留原有 PowerShell/WinForms 入口、稳定项目 ID、清理计划哈希、时间与大小快照校验。
- 微信和 QQ 图片视频仍为谨慎项目，默认不选择；执行后移入系统回收站，清空前可恢复，且不能与清空回收站同批执行。
- 程序按当前权限运行，不自动触发 UAC；需要管理员权限的项目会跳过并记录。
- 云端助手只允许在用户明确同意后读取字段白名单生成的脱敏摘要，不传原始路径、用户名、日志或文件内容。
- AI、NTFS 加速扫描和云端服务均为可选能力，不影响离线基础清理功能。
- 全量 .NET 重写暂缓；当前只下沉已测量的性能热点，迁移触发阈值和回滚路径已机器化记录。
