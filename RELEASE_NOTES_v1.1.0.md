# C盘智能清理 v1.1.0

发布日期：2026-08-19

## 安全与可靠性

- 所有扫描与清理目标改用显式、稳定且唯一的 ID，新增项目不会改变已有目标的身份。
- 清理清单升级为计划协议 v2，包含扫描编号、规则清单 SHA-256、扫描时间和项目大小快照。
- 清理计划有效期为 30 分钟；规则变化、计划缺项或微信/QQ 用户内容变化时，会安全中止并要求重新扫描。
- 普通缓存发生自然大小波动时重新核验固定范围，不再因后台写入少量缓存而反复阻断。
- UI 根据主程序退出码区分成功、失败和安全中止，不再把失败任务显示为清理完成。

## 数据目录

- HTML 报告迁移至 `%LOCALAPPDATA%\CDriveCleaner\reports`。
- 程序目录只保存应用与资源，便于未来安装、升级和回滚。
- 扫描计划、选择文件和运行日志会在任务完成或窗口退出时清理。

## 发布工程

- 发布流程拆分为只读构建和最小写权限发布两个任务。
- GitHub Actions 固定到经过核实的提交 SHA。
- 最新更新元数据统一使用 GitHub Release 内自动生成的 `release.json`，不再维护容易过期的仓库根目录副本。
